# Automation
Various Projects Involving Python Automation

Experimentation in Python Automation... Used Apple Script to send messages via iMessage, as well as PyAutoGUI to write and send messages. 

Made a script to proccess data from a JSON then open a specific WhatsApp user using subprocess/shell and creating message from JSON data and sending use PyAutoGUI.

Used AppleSscript to send the Emoji Movie script word by word to someone through iMessage (auto spammer).

Used PyAutoGUI to automate sending messages given a communication platform and text field.
